<figure class="flex items-center justify-center">
  <img
    class="abosolute object-cover w-screen h-[760px]"
    src="@/../public/rowing.jpg"
    alt="Imagen de modelo"
  />
  <section class="absolute text-white">
    <div class="absolute inset-0"></div>
    <div
      class="bg-black bg-opacity-10 relative z-10 flex flex-col items-center justify-center h-full text-center"
    >
      <h1 class="text-7xl font-bold mb-4">¡Transforma Tu Cuerpo!</h1>
      <p class="text-lg text-neutral-400 mb-4">
        Atrévete a romper tus límites en el mejor gimnasio de la ciudad.
      </p>
      <a
        href="#"
        class="text-white bg-red-800 border-2 border-red-900 hover:bg-red-900 hover:border-dark font-bold rounded-lg text-[15px] px-4 py-2 text-center"
        >Afiliate</a
      >
    </div>
  </section>
  <div class="scroll-indicator">
    <span class="arrow"></span>
    <span class="arrow"></span>
  </div>
</figure>
<style>
    h1{
        font-family: "Oswald", sans-serif;
    }

    .scroll-indicator {
    position: absolute;
    display: flex;
    flex-direction: column;
    align-items:center;
    justify-content: end;
    height: 600px;
  }

  .arrow {
    width: 30px;
    height: 30px;
    border-bottom: 5px solid #ffffff;
    border-right: 5px solid #ffffff;
    transform: rotate(45deg);
    margin: -10px;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%,
    20%,
    50%,
    80%,
    100% {
      transform: translateY(0) rotate(45deg);
    }
    40% {
      transform: translateY(10px) rotate(45deg);
    }
    60% {
      transform: translateY(5px) rotate(45deg);
    }
  }
</style>

